{
  "name": "new-kanaga-portail-evaluation",
  "version": "1.0.0",
  "description": "Google Apps Script project for Kanaga Portail Evaluation",
  "scripts": {
    "pull": "clasp pull",
    "push": "clasp push",
    "watch": "clasp push --watch",
    "open": "clasp open",
    "deploy": "clasp deploy"
  },
  "devDependencies": {
    "@google/clasp": "^3.4.1"
  }
}
